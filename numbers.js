const score = 400;
// another way to write number
const balance = new Number(100);
console.log(score);
console.log(balance);
console.log(balance.toString());// converts to string
console.log(balance.toString().length);// this way u can find the length
console.log(balance.toFixed(3)); //used for round off
const num = 1298263452348279834;
console.log(num.toLocaleString());// provides comma in indian standard standard